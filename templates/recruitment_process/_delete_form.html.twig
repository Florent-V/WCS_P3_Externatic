<form method="post" action="{{ path('app_recruitment_process_delete', {'id': recruitment_process.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recruitment_process.id) }}">
    <button class="btn">Delete</button>
</form>
