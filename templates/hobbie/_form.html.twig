{{ form_start(hobbieForm) }}
            {{ form_row(hobbieForm.title, {
                label: 'Nom du Hobby',
                attr: {
                    placeholder: 'Nom du Hobby'
                },
                row_attr: {
                    class: 'form-floating mb-3'
                }
            }) }}

<button class="btn">{{ button_label|default('Ajouter') }}</button>
{{ form_end(hobbieForm) }}
