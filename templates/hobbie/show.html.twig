{% extends 'base.html.twig' %}

{% block title %}Hobbie{% endblock %}

{% block body %}
    <h1>Hobbie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hobbie.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ hobbie.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hobbie_index') }}">back to list</a>

    <a href="{{ path('app_hobbie_edit', {'id': hobbie.id}) }}">edit</a>

    {{ include('hobbie/_delete_form.html.twig') }}
{% endblock %}
