<form class="deleteForm" method="post" action="{{ path('admin_app_company_delete', {'id': company.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company.id) }}">
    <button class="btn"><i class="bi bi-trash3-fill"></i></button>
</form>
