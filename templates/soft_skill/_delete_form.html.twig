<form method="post" action="{{ path('app_soft_skill_delete', {'id': softSkill.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ softSkill.id) }}">
    <button class="btn"><i class="bi bi-x"></i></button>
</form>
