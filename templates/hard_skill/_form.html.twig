{{ form_start(hardSkillForm) }}
            {{ form_row(hardSkillForm.name, {
                label: 'Compétence',
                attr: {
                    placeholder: 'Nom de la compétence'
                },
                row_attr: {
                    class: 'form-floating mb-3'
                }
            }) }}

<button class="btn btn-primary">{{ button_label|default('Ajouter') }}</button>
{{ form_end(hardSkillForm) }}
