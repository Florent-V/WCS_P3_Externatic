{% extends 'base.html.twig' %}

{% block title %}Mes annonces{% endblock %}

{% block body %}
    <main class="container-md search" {{ stimulus_controller('favoriteoffer') }}>

        {% for annonce in fetchedAnnonces %}
            <div class="row row-cols-xl-3 row-cols-md-2 favoriteAnnounceList">
                <div class="col mt-4 cards-result">
                    {% include '_include/_card_result.html.twig' %}
                </div>
            </div>

        {% else %}
            <div class="oups">

                <img src="{{ asset('build/images/oups.jpg') }}" alt="illustration page vide"/>
                <p>
                    Oh ! Il semblerait qu'il n'y ait encore rien ici !
                    <br>
                    Mets des annonces en favori et elles apparaîtront ici.
                </p>
            </div>
        {% endfor %}

    </main>

{% endblock %}


