{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block title %}Ajouter une annonce {% endblock %}

{% block body %}
    <div class="annonce-title">
        <h1>
            Ajouter une annonce
        </h1>
    </div>

    <div class="form-annonce">

        {{ form_start(form) }}
        {{ form_widget(form) }}

        <div class="btn"><button class="btn">{{ button_label|default('Mettre l\'annonce en ligne') }}</button></div>
        {{ form_end(form) }}
    </div>

{% endblock %}

