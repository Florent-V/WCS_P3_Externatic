<form method="post" action="{{ path('app_certification_delete', {'id': certification.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ certification.id) }}">
    <button class="btn"><i class="bi bi-trash3-fill"></i></button>
</form>
